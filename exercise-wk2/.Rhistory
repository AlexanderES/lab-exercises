# Make a vector and use typeof to check what type R considers it to be
v1 <- (1, 2, 3, 4, 5)
v2 <- c(1, 2, 3)
# Make a vector and use typeof to check what type R considers it to be
v1 <- c(1, 2, 3, 4, 5)
typeof(v1)
